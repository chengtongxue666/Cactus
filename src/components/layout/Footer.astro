import { menuLinks, siteConfig } from "@/site.config";

const year = new Date().getFullYear();const year = new Date().getFullYear();
---

<footer<footer
	class="mt-auto flex w-full flex-col items-center justify-center gap-y-2 pb-4 pt-20 text-center align-top font-semibold text-gray-600 dark:text-gray-400 sm:flex-row sm:justify-between sm:text-xs"	class="mt-auto flex w-full flex-col items-center justify-center gap-y-2 pb-4 pt-20 text-center align-top font-semibold text-gray-600 dark:text-gray-400 sm:flex-row sm:justify-between sm:text-xs"
>
	<div class="me-0 sm:me-4">	<div class="me-0 sm:me-4">
		&copy; {siteConfig.author}		&copy; {siteConfig.author}
		<!-- 修改 -->		<!-- 修改 -->
		<!-- {year}.<span class="inline-block">&nbsp;🚀&nbsp;Astro Cactus</span> -->		<!-- {year}.<span class="inline-block">&nbsp;🚀&nbsp;Astro Cactus</span> -->
		{year}		{year}
	</div>	</div>
	<nav	<nav
		aria-label="More on this site"		aria-label="More on this site"
		class="flex gap-x-2 sm:gap-x-0 sm:divide-x sm:divide-gray-500"		class="flex gap-x-2 sm:gap-x-0 sm:divide-x sm:divide-gray-500"
	>
		{
			menuLinks.map((link) => (			menuLinks.map((link) => (
				<a class="px-4 py-2 sm:py-0 sm:hover:text-textColor sm:hover:underline" href={link.path}>				<a class="px-4 py-2 sm:py-0 sm:hover:text-textColor sm:hover:underline" href={link.path}>
					{link.title}					{link.title}
				</a>				</a>
			))
		}
	</nav>	</nav>

</footer></footer>
